<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jfsoft.mapper.TRoleMapper" >
	
	<resultMap id="BaseResultMap" type="com.jfsoft.model.Role" >
		<result column="id" property="id" jdbcType="INTEGER" />
		<result column="rolename" property="rolename" jdbcType="VARCHAR" />
	</resultMap>

	<sql id="Base_Column_List" >
		id, rolename
	</sql>

	<insert id="insert" parameterType="com.jfsoft.model.Role" >
		insert into t_role (id, rolename)
		values (#{id,jdbcType=INTEGER}, #{rolename,jdbcType=VARCHAR})
	</insert>

	<insert id="insertSelective" parameterType="com.jfsoft.model.Role" >
		insert into t_role
		<trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="id != null" >
				id,
			</if>
			<if test="rolename != null" >
				rolename,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="id != null" >
				#{id,jdbcType=INTEGER},
			</if>
			<if test="rolename != null" >
				#{rolename,jdbcType=VARCHAR},
			</if>
		</trim>
	</insert>

	<!-- 根据角色查询权限 -->
	<select id="getPermissions" parameterType="java.lang.String" resultMap="BaseResultMap">
		SELECT
			tp.id,tp.permissionname
		FROM
			t_permission tp
		WHERE
			tp.role_id=#{roleId}
	</select>

</mapper>
